{
  "name": "sistema-di-frammenti",
  "version": "1.0.0",
  "description": "Sito web statico per la tesi «Sistema di frammenti» di Alessandro Pinna",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve build --listen 3000",
    "dev": "node build.js && npx serve build --listen 3000"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "marked": "^12.0.0"
  }
}
